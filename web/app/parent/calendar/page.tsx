import { CalendarPage } from '@/components/calendar/calendar-page';

export default function ParentCalendarPage() {
  return (
    <CalendarPage description="Term dates, holidays, and school events. RSVP to events that need a response." />
  );
}
